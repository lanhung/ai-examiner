# AI Examiner v0.10.0 — 考试窗口与课堂试点

发布日期：2026-09-24

## 这一版能做什么

- **老师在工作台发布考试**：上传材料、生成蓝图后，在「发布给学生」中选择练习或考试模式、题目数、作答次数、截止时间和是否需要学号，发布后得到 6 位入口码和链接，可直接发到微信群。
- **学生用手机直接作答**：打开链接或输入入口码即可开始，不需要注册。页面只有三屏：入口、对话、结果。关掉微信后重新打开同一链接，会接着上次的进度。
- **练习与考试两种模式**：
  - 练习模式：有提示，提交后立即看到结果。
  - 考试模式：全程没有提示、纠正和答案泄露，学生提交后只显示「已提交」，老师公布后才能看到结果。
- **老师查看作答**：每个学生的状态、回答数和平均得分；可以查看完整对话，导出 CSV，关闭或重新开放考试，公布结果。

## 稳定性（均已实测）

- 压测：40 名学生同时作答全部完成，0 错误；80 名学生同时作答也全部完成。修复了几个在上课时会出错的问题：
  - 同时加入时偶发的服务器错误；
  - 超过模型并发上限时直接报错（现在改为排队）；
  - 数据库连接池耗尽；
  - 同一学校网络下被误限流。
- 生产环境试点用 Docker 完整演练过：冒烟测试全部通过，浏览器全流程通过，40 名学生经 Caddy 访问全部完成，备份完整性检查通过，重启后数据仍在。
- CI 恢复全绿：自 v0.9.0 起一直失败，原因是锁文件与版本号不一致、依赖存在已公开的漏洞、PostgreSQL 测试无法重建数据表。Docker 镜像现在严格按锁文件安装依赖。

## 升级说明

- 新的数据库迁移 `20260924_0018` 在启动时自动执行。
- 新增课堂试点部署方式（`USE_PILOT=true`），见上线手册。
- 回滚到 v0.9.x 时必须同时恢复升级前的备份：`./deploy/rollback.sh v0.9.1 <备份文件>`。
- 如使用企业版（OIDC）部署，只需按原方式升级；新增权限 `assignment.manage` / `assignment.read` / `assignment.attempt` 已自动分配给相应角色。

## 已知限制

- 真实大模型的多模型对比评测尚未运行，需要在有 API Key 的服务器上执行 quality lab。
- 试点模式只有一个老师账号；需要多位老师分别登录时，请使用企业版部署。
- 工作台暂时没有删除项目的按钮（可以用命令删除，见上线手册）。
