# ADR-001：第一版采用模块化单体

状态：Accepted

## 决策

FastAPI 同时提供 API、静态网页和 Agent 编排；SQLite 和本地文件系统保存 POC 数据。

## 原因

- 单人 + AI Agent 最容易运行、调试和打包；
- 不需要维护前后端两个构建系统；
- 状态机和行为评测比基础设施规模更重要；
- 后续模块边界已清晰，可独立迁移到 PostgreSQL、对象存储和媒体服务。

## 代价

- 不适合高并发和多租户；
- 静态网页功能有限；
- 生产化前必须补充认证、权限、HTTPS、备份和审计。
